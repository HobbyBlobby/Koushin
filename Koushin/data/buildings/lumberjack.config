[general]
name=Lumberjack

[tasks][globals]
forester=10

[fieldTasks][increaseWood]
needs=field(fieldWithForest)
numberOfFields=2
numberOfFieldsPerLevel=0.5
fieldRadius=2
increaseFieldRadius=0.5
recipient=field()
action=gatherResource(Wood, 10)
